<Window x:Class="DeviantartDownloader.Views.SettingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeviantartDownloader.Views"
        mc:Ignorable="d"
        Title="Cookie Setting" Height="500" ResizeMode="NoResize" Width="800"
        WindowStartupLocation="CenterScreen"
        xmlns:vm="clr-namespace:DeviantartDownloader.ViewModels" d:DataContext="{d:DesignInstance Type=vm:SettingViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="375"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <StackPanel Margin="20,0,20,0" Grid.Column="0" >
                <Label Content="Header string:" Target="{Binding ElementName=txtContent}" />
                <TextBox Name="txtContent" Background="White" 
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     TextWrapping="Wrap"
                     Height="325" FontSize="18" Text="{Binding HeaderString,UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,0,0"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="20,0,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0">
                Queue limit:
            </TextBlock>
            <TextBox Grid.Column="1" MaxLength="1" Text="{Binding QueueLimit,UpdateSourceTrigger=PropertyChanged}" Width="40" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" PreviewTextInput="TextBox_PreviewTextInput" PreviewKeyDown="TextBox_PreviewKeyDown" />
            <TextBlock Grid.Column="2" Margin="5,0,0,0">
                Concurrent download
            </TextBlock>
        </Grid>
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Width="200" Height="40" Click="Button_Click" >Close</Button>
            <Button Grid.Column="1" Width="200" Height="40" Command="{Binding SaveCommand}">Save</Button>
        </Grid>
    </Grid>
</Window>
