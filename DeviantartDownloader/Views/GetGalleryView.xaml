<Window x:Class="DeviantartDownloader.Views.GetGalleryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeviantartDownloader.Views" xmlns:viewmodels="clr-namespace:DeviantartDownloader.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:GetGalleryViewModel}"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="GetGallery" Height="800" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="90"/>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid VerticalAlignment="Top" Grid.Row="0" Margin="20,10,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Label Content="Search from username:" Target="{Binding ElementName=textUsername}" />
                <TextBox Name="textUsername"  Background="White"  Height="30" FontSize="18" Text="{Binding UserName}"  Margin="20,0,20,0"/>
            </StackPanel>
            <Button Command="{Binding GetFolderCommand}" Grid.Column="1"  Content="{Binding loadingSearchFolder}"/>
        </Grid>
        <StackPanel Grid.Row="1" Margin="20,0,20,0">
            <Label Content="User folder" Target="{Binding ElementName=cbFolder}" />
            <Grid  Margin="20,0,20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Margin="0,0,10,0">Selected user: </TextBlock>
                <TextBlock Margin="10,0,0,0" Grid.Column="1" Text="{Binding selectedUsername}"/>
                <Button Command="{Binding ResetUserCommand}" Grid.Column="2" Content="Reset"/>
            </Grid>
            <ComboBox 
                IsSynchronizedWithCurrentItem="True"
                Name="cbFolder"
                ItemsSource="{Binding Folders}"
                DisplayMemberPath="DisplayName"
                SelectedValuePath="Id"
                SelectedItem="{Binding selectedFolder,Mode=TwoWay}"
                Margin="20,0,20,0"/>
        </StackPanel>
        <Grid Grid.Row="2" Margin="0,20,0,0" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <Button Name="Btn_Cancel" Grid.Column="0" Margin="0,0,20,0" Click="Btn_Cancel_Click">Close</Button>
            <Button Name="Btn_AddList" Grid.Column="1" Margin="0,0,20,0"  Command="{Binding GetDeviantCommand}" Content="{Binding loadingSearchDeviant}"/>
        </Grid>
        <DataGrid  CanUserAddRows="False" RowHeight="30" ItemsSource="{Binding Deviant}" AutoGenerateColumns="False" Grid.Row="3" Margin="20,20,20,20" SelectionMode="Single" CanUserResizeRows="False">
            <DataGrid.Columns>
                <!-- Other DataGridTextColumns can go here -->
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Author.Username}" Width="200" Header="Author">
                   
                </DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Title}"  Width="*" Header="Deviation Name">
                    
                </DataGridTextColumn>
                <DataGridTextColumn IsReadOnly="True" Width="60" Binding="{Binding Type}" Header="Type">

                </DataGridTextColumn>
                <DataGridTemplateColumn Header="" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding Path=DataContext.RemoveDeviantFromListCommand,       
                                    RelativeSource= {RelativeSource FindAncestor,
                                    AncestorType={x:Type DataGrid}}}" CommandParameter="{Binding Path=Deviationid}" Content="Remove"  />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            
        </DataGrid>
        <Button Name="Btn_Submit" Grid.Row="4" Margin="10,10,10,10" Content="Add to download list" Command="{Binding SubmitToDownloadListCommand}" Click="Btn_Submit_Click"/>
    </Grid>
</Window>
