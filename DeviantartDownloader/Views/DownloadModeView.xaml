<mah:MetroWindow x:Class="DeviantartDownloader.Views.DownloadModeView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:local="clr-namespace:DeviantartDownloader.Views"
                 xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                 xmlns:vm="clr-namespace:DeviantartDownloader.ViewModels"
                 d:DataContext="{d:DesignInstance Type=vm:DownloadModeViewModel}"
                 xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
                 mc:Ignorable="d"
                 Title="Download mode" Height="275" Width="800"
                 ResizeMode="NoResize" ShowIconOnTitleBar="False"
                 WindowStartupLocation="CenterScreen" WindowTitleBrush="#06070d"
                 Background="#d4dfd0" BorderBrush="Black"
                 Dialog:DialogParticipation.Register="{Binding}">    
    <Grid RowDefinitions="70,30,70,*">
        <StackPanel Grid.Row="0" 
                    Margin="20,0,20,0">
            <Label Content="Deviantion Type:"/>
            <ComboBox mah:TextBoxHelper.Watermark="Empty"
                      mah:ControlsHelper.CornerRadius="4"
                      Foreground="#337287"
                      ItemsSource="{Binding TypeModes}"
                      SelectedValue="{Binding SelectedType,UpdateSourceTrigger=PropertyChanged}"                
                      DisplayMemberPath="Display"
                      SelectedValuePath="Type"
                      FontSize="16"
                      BorderBrush="#93a98f"
                      Margin="20,0,20,0"
                      Cursor="Hand" FontWeight="SemiBold"
                      Background="#e9f0e6"/> 
        </StackPanel>
        <Label Grid.Row="1"  
               HorizontalAlignment="Center" Margin="20,0,20,0" 
               Content="AND"/>
        <StackPanel Grid.Row="2" 
                    Margin="20,0,20,0">
            <Label Content="Status:"/>
            <ComboBox mah:TextBoxHelper.Watermark="Empty"
                      mah:ControlsHelper.CornerRadius="4"
                      Foreground="#337287"
                      ItemsSource="{Binding StatusModes}"
                      SelectedValue="{Binding SelectedStatus,UpdateSourceTrigger=PropertyChanged}"                
                      DisplayMemberPath="Display"
                      SelectedValuePath="Status"
                      FontSize="16"
                      BorderBrush="#93a98f"
                      Margin="20,0,20,0"
                      Cursor="Hand" FontWeight="SemiBold"
                      Background="#e9f0e6"/>
        </StackPanel>
        <Button Grid.Row="3"   
                mah:ControlsHelper.CornerRadius="2"
                mah:ControlsHelper.ContentCharacterCasing="Upper"
                mah:ControlsHelper.FocusBorderBrush="#93a98f"
                mah:ControlsHelper.MouseOverBorderBrush="#93a98f"
                Style="{StaticResource MahApps.Styles.Button.Square.Accent}"
                Command="{Binding SaveCommand}"
                Margin="20,20,20,10"
                BorderBrush="#93a98f" BorderThickness="2"
                FontSize="20" Cursor="Hand"
                Content="Save"/>
    </Grid>
</mah:MetroWindow>
