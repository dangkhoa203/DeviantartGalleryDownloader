<Window x:Class="DeviantartDownloader.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeviantartDownloader"
        xmlns:vm="clr-namespace:DeviantartDownloader.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Background="AntiqueWhite"
        >
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="50" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid VerticalAlignment="Top" Grid.Row="0" Margin="10,10,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Label Content="Path:" Target="{Binding ElementName=txtPath}" />
                <TextBox Name="txtPath" Background="White"  Height="30" FontSize="18" Text="{Binding DownloadPath}"  Margin="0,0,20,0"/>
            </StackPanel>
            <Button Command="{Binding GetDownloadPathCommand}" Grid.Column="1" Height="50" Content= "Path"/>
        </Grid>
        <Grid Grid.Row="1" Margin="20,0,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Margin="20,0,20,0" Grid.Column="0" Content="Get from gallery"/>
            <Button Margin="20,0,20,0"  Grid.Column="1" Content="Clear"/>
            <Button Margin="20,0,20,0"  Grid.Column="2" Content="Download"/>
        </Grid>
        <Grid Grid.Row="2">
            
        </Grid>
    </Grid>
</Window>
