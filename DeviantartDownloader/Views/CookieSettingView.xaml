<Window x:Class="DeviantartDownloader.Views.CookieSettingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DeviantartDownloader.Views"
        mc:Ignorable="d"
        Title="CookieSettingView" Height="500" ResizeMode="NoResize" Width="800"
        WindowStartupLocation="CenterScreen"
        xmlns:vm="clr-namespace:DeviantartDownloader.ViewModels" d:DataContext="{d:DesignInstance Type=vm:CookieSettingViewModel}"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="400"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <StackPanel Margin="20,0,20,0" Grid.Column="0" >
                <Label Content="Header string:" Target="{Binding ElementName=txtContent}" />
                <TextBox Name="txtContent" Background="White" 
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         TextWrapping="Wrap"
                         Height="350" FontSize="18" Text="{Binding HeaderString,UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,0,0"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Width="200" Height="40" Click="Button_Click" >Close</Button>
            <Button Grid.Column="1" Width="200" Height="40" Command="{Binding SaveCommand}">Save</Button>
        </Grid>
    </Grid>
</Window>
